# Base URL used by the browser when calling the API.
# Leave empty in local dev and the Vite dev server will proxy /api/* and /actuator/* to http://localhost:8084.
VITE_API_BASE_URL=

# Target used by the dev proxy. Change if your Spring Boot server runs on a different host/port.
VITE_API_PROXY_TARGET=http://localhost:8084
